#include <iostream>
int main(){
  int a = 10;
  if (a>$V1=10,step=0.5,type=float$)
    std::cout<<"变异条件小于10";
  if (a<$V2=10,step=0.5,type=float$)
    std::cout<<"变异条件大于10";
#if $enum1=1,opt=0|1,chance=0.1$
  if(a$compC1=<,chance=0.1$10)std::cout<<"运算符发生变异"
#endif
}